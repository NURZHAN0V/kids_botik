def функция_виселица():
    import random
    import os

    def очистить_экран():
        os.system('cls' if os.name == 'nt' else 'clear')

    слова = {
        "легкий": ['кот', 'пёс', 'дом', 'лес', 'свет'],
        "средний": ['стол', 'луна', 'река', 'ночь', 'тень'],
        "сложный": ['пример', 'улыбка', 'картина', 'решение']
    }

    while True:
        print("Добро пожаловать в Виселицу!")
        уровень = input("Выбери уровень (лёгкий/средний/сложный): ").lower()
        if уровень in слова:
            break
        else:
            print("Неверный уровень. Попробуй ещё раз.")

    слово = random.choice(слова[уровень])
    угадано = ['_'] * len(слово)
    осталось_попыток = 7 if уровень == "лёгкий" else 5 if уровень == "средний" else 3

    while '_' in угадано and осталось_попыток > 0:
        очистить_экран()
        print(f"Осталось попыток: {осталось_попыток}")
        print(' '.join(угадано))
        буква = input("Угадай букву: ").lower()

        if len(буква) != 1 or not буква.isalpha():
            print("Пожалуйста, введи одну букву.")
            input("Нажми Enter, чтобы продолжить...")
            continue

        if буква in угадано:
            print("Эта буква уже открыта.")
            input("Нажми Enter, чтобы продолжить...")
            continue

        if буква in слово:
            for i in range(len(слово)):
                if слово[i] == буква:
                    угадано[i] = буква
        else:
            осталось_попыток -= 1
            print("Нет такой буквы.")

    очистить_экран()
    if '_' not in угадано:
        print(f"🎉 Поздравляю! Ты угадал слово: {слово}!")
    else:
        print(f"💀 Ты проиграл. Слово было: {слово}")

    ещё = input("Хочешь сыграть ещё раз? (да/нет): ").lower()
    if ещё == "да":
        функция_виселица()
    else:
        print("Спасибо за игру! До новых встреч!")